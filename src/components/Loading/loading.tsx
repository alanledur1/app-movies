// components/Loading/Loading.tsx
import React from 'react';
import './loading.scss'; // Arquivo de estilos para o componente

const Loading: React.FC = () => {
    return (
    <div className="loader"></div>
    );
};

export default Loading;
